import {CustomSectorRuleInfo} from './custom-sector-rule-info.model';

describe('CustomSectorRuleInfo', () => {
    it('should create an instance', () => {
        expect(new CustomSectorRuleInfo()).toBeTruthy();
    });
});
