import {SectorRuleInfo} from './sector-rule-info.model';

describe('SectorRuleInfo', () => {
    it('should create an instance', () => {
        expect(new SectorRuleInfo()).toBeTruthy();
    });
});
