/**
 * All comparison types supported by highlight rules.
 */
export enum HighlightComparisonType {
    EQUALS,
    DOES_NOT_EQUAL,
    GREATER_THAN,
    LESS_THAN,
    GT_THAN_EQUAL,
    LESS_THAN_EQUAL,
    STARTS_WITH,
    CONTAINS,
    DOES_NOT_CONTAIN,
    TOP,
    BOTTOM,
    BETWEEN,
    STD_DEV_IN,
    STD_DEV_OUT,
    QUANTILE
}
