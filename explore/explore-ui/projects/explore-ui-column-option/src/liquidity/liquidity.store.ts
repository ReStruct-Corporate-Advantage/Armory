/**
 * Liquidity Store
 */
export class LiquidityStore {
    // liquidityDefaults (from portfolio.portfolioDefaults.liquidityDefaults) gets set when current portfolio is updated
    static liquidityDefaults = null;
}
