/**
 * Enum for mode of measure in style analysis collumns
 */
export enum StyleMeasureMode {
    // Style Measure Mode in UNSPECIFIED
    STYLE_MEASURE_MODE_UNSPECIFIED = 0,
    // Style Measure Mode in NORMAL
    STYLE_MEASURE_MODE_NORMAL = 1,
    // Style Measure Mode in INVERSE
    STYLE_MEASURE_MODE_INVERSE = 2,
}
