/**
 * Enum for look through settings changed type event
 */
export enum LookThroughSettingsChangeType {
    SECURITY_TYPE_CHANGED = 0,
    LOOKTHROUGH_ENABLED = 1,
    LOOKTHROUGH_DISABLED = 2,
    ENABLED_TYPE = 3
}
