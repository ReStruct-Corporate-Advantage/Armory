
/**
 * Enum class for batch row export run as options
 */
export enum BatchExportRunAs {
    PORTGROUP, // Run the export as a port group
    PORTFOLIOS // Run the export as portfolios (all underlying)
}
