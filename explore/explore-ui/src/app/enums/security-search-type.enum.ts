/**
 * This enum is for the security search types.
 */
export enum SecuritySearchTypeEnum {
    BY_SECURITY = 'By security',
    FROM_PORTFOLIOS = 'From portfolios',
    FROM_ISHARES = 'From iShares'
}
