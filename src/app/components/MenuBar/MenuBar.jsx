import React from "react";
import PropTypes from "prop-types";
import "./MenuBar.component.scss";

const MenuBar = props => {
  return (
    <div className="c-MenuBar">
      In Component MenuBar
    </div>
  );
};

MenuBar.propTypes = {

};

export default MenuBar;