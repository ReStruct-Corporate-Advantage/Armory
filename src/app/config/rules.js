const RULES = {
    header: {
        rule: "edge",
        default: "top"
    },
    aside: {
        rule: "edge",
        default: "left"
    },
    list: {
        rule: "stacked",
        default: "vertical" // vertical stack list items
    }
}

export default RULES;