import { useState } from "react"

const useTool = (toolName, props) => {
    const [showQuickSettings, setShowQuickSettings] = useState(false);
    const [showNotifications, setShowNotifications] = useState(false);

    const notifications = () => {

    }

    const toggles = () => {

    }

    // const 

    return toolName
}
export default useTool